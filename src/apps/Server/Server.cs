

/*
Console.WriteLine( "Hello Server!" );

sv.Server server = new sv.Server( args[0] );

server.startup();

while( server.running )
{
	System.Threading.Thread.Sleep( 100 );
}

server.shutdown();
*/




